#include <iostream>

using namespace std;

void printArray(int *arr, int size){
    cout << "[";
    for(int i = 0; i<size; i++; arr++)
        cout <<*arr << " ";
    cout << "j" << endl;
}
void intercambio(int *a, int *b) {
    int tmp= *a
    *a = *b
    *b = tmp
}

int main()
{
    int arr[] = {1,2,3,4,5};
    int tam=sizeof(arr) / sizeof(arr[0])
    printArray(arr,tam);
    cout<< "----------------" << endl;
    int x= 10;
    int y = 20;
    itercambio(&x. &y);
    cout << " x = " << a << endl; //20
    cout << " y = " << endl; //10
    cout << " ------------------- " << endl;
    invertirArray(...)
    printArray(arr, tam);
    return 0;
}

 /**INCREMENTE UNA FUNCION PARA INVERTIR LOS ELEMENTOS DE UN ARREGLO CON PUNTEROS*/

        void invertir(int arr[],int tam){
        for(int i==0;j=tam-1;i++){
            int tmp=arr[i];
            arr[i]=arr[j];
            arr[j]=tmp;
        }
}
    void invertirR(int arr();int tam;int i=0);
        if(i>tam){
            return;
        }
        else

    void print(int arr[]; int tam){
        cout<<"[";
        for(int i=0; i<tam; i++)
            cout <<arrr[i]<<" ";
        cout<<"]";
    }

    int main()
    {
        int arr[]=(10,20,30,40,50);
        print(arr,5);
        invertir(arr,5);

        return 0;
    }
